export enum TaskStatusId {
  New = 1,
  InProgress = 2,
  Done = 3,
  Cancelled = 4,
  Failed = 5,
}

export enum TaskStatus {
  New = 'new',
  InProgress = 'in-progress',
  Done = 'done',
  Cancelled = 'cancelled',
  Failed = 'failed',
}
