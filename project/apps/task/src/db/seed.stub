import { Knex } from 'knex';

const tableName = '';

export async function seed(knex: Knex): Promise<any> {
  await knex(tableName)
      .del()
      .insert([
        {
          id: 1,
        },
      ]);
}
